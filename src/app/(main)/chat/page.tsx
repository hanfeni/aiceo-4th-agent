// Slice 1 스텁 — Slice 8(Wave 5)에서 ChatPanel + 레이아웃으로 교체된다.
// plan.md: Slice 8 Files 에 이 경로 명시. 빌드 통과용 최소 페이지.
export default function ChatPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>DeepAgents Chat</h1>
      <p>scaffold ready — chat UI는 Slice 8에서 구현됩니다.</p>
    </main>
  );
}
