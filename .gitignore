# dependencies
node_modules/
.pnpm-store/

# next.js (루트 빌드 산출물만 — src/.../build/ 라우트는 추적)
.next/
out/
/build/
next-env.d.ts
*.tsbuildinfo

# checkpointer (LangGraph SQLite — NFR-8)
.data/

# OpenSearch 검색 실습 데이터 볼륨 (인덱스 = 원문 코퍼스 역추출 가능 →
# 제3자 저작권 본문. 공개 리포 유출 치명. docker-compose.opensearch.yml)
.opensearch-data/

# env (서버 전용 키 — 절대 커밋 금지)
.env
.env.local
.env.*.local

# test / coverage
coverage/
test-results/
playwright-report/
.playwright/
.playwright-mcp/

# Claude Code 로컬 하네스 설정 (교육용 — 추적 제외)
.claude/

# misc
.DS_Store
*.log
/tmp/
